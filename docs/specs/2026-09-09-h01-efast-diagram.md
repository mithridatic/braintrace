# H01 EFAST diagram

Create `docs/h01-efast.svg` beside the causal model.
This is a document asset, with no generator or model code change.

Use Hartshorne Figures 301–304, 313, and 314 from the supplied page images.
HOW points left. WHY points right. Shared junctions indicate AND relationships.
Vertical support links use the book's IF–THEN convention.
Use an OR relationship only for actual alternative functions.

The scope is one modeled cell and its incoming synapses.
The same map applies to every modeled cell.
Applied current, ionic current, axial current, and synaptic current change membrane charge.
Capacitance relates membrane charge to voltage.
Conductance and driving force jointly determine each ohmic current.
Gate state and calcium history affect channel conductance.
Source-event delivery and receiving voltage affect synaptic current.

Colors indicate proposed measurement boundaries, not passed tests.
Retain ion-gradient energy sources and membrane energy storage.
Do not imply that calcium concentration is an energy displacement variable.
Do not separate spike rate, width, and recovery into independent mechanisms.
No status table, campaign history, test scores, or commentary below the diagram.

Verify SVG syntax and render the final asset.
Check labels, AND junctions, scope boundaries, overlap, and clipping.
Link the asset from `docs/h01-causal-model.md`.
