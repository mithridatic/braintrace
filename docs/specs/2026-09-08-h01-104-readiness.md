# H01 all-104 readiness continuation

Authorized by the user on 2026-09-08: implement the proposed reader repair,
verify all 104 imports, and proceed autonomously toward full population readiness.
The existing population programme's physiological and numerical gates remain open.
Twelve simulated cells is a baseline, not completion of this objective.

## First blocking defect

BrainCell compares attachment coordinates with relative tolerance. At H01's
absolute coordinates this can discard a real 0.032 um segment. Seven largest
components fail loading, including a short branch with children. Do not collapse
nodes, translate anatomy, join fragments, or fabricate cable lengths.

Use an H01-local reader specialization to explicitly retain distinct attachment
coordinates when the upstream approximate comparison would discard them. Keep
the upstream validation pipeline and branch constructor. Do not patch installed
dependencies or modify the unrelated dirty performance worktree.

## Verification

First reproduce leaf and branching-node failures with translated synthetic
geometry. Assert cable lengths, coordinates, topology and source provenance;
retain tests for invalid input and ordinary imports. Run the relevant importer
suite with coverage above 90 percent on new production code. Then audit imports
of all 104 selected population components against source cable geometry.

After import qualification, benchmark construction and initialization in stages
through 104 cells, then compiled multi-step runs and matched controls. Record
measured time and memory, finite per-cell traces, and enabled contacts. Preserve
donor provenance and report physiological gates independently. Full readiness
requires the outstanding population programme qualification gates; an import
pass alone is insufficient. Example 21 implementation remains deferred.

## Execution authority and next measurements

The user subsequently granted "Full authority" and autonomous execution.
Proceed with the staged runs without repeating per-job approval questions;
retain measured estimates, resource checks and explicit qualification limits.
Do not resume capped physiological parameter searches without a new bounded
causal specification.

After the import audit passes, retry the existing 40-cell build-only command,
with 10 um CV length, the h01_staggered_scan solver, existing donor mapping,
1 nA input configuration and the original topology. The previous projection
from 12 cells was 590 s construction and 3.8 GB peak memory, not a measurement.
Use a 1,500 s wall cap and 600 s silence cap; abort means untested. If successful,
run init-only, then a compiled 1 ms smoke and matched controls, with the prior
1,800 s per-launch cap. Update estimates from these measurements before 104.
Keep a minimum 4 GB host memory reserve. The all-104 goal is not reduced if a
resource cap triggers: diagnose the resource cost and continue toward 104.

The unchanged 1 nA pulse starts at 2 ms, so a 1 ms smoke run cannot demonstrate
driven firing or functional inhibition. Longer windows and delivery controls
remain required; no event qualification is inferred from a finite smoke.

## 104-cell preflight (registered before launch)

The plan contains exactly all 104 component-inventory identities. The audited
source node totals are 537,152 at 40 cells and 2,804,549 at 104, a ratio of
5.221146. Scaling the measured 40-cell init run gives 1,344.7 seconds construction,
1,180.9 seconds initialization and 12,879.9 MiB peak resident memory. These are
heuristic projections, not measured 104-cell performance. Evidence:
`docs/evidence/h01-ready-104-preflight.json`.

After the 40-cell compiled smoke passes, launch the 104-cell build-only stage
with the same topology, 10 um CV limit, donor registry, solver and assumed input
configuration. Freshly check available memory against the projected peak plus
4 GiB reserve. Set a 3,600-second wall cap and 600-second silence cap; stop if
available memory falls below 4 GiB. Preserve partial evidence on an abort and
diagnose the limiting stage without reducing the all-104 objective.

The build gate requires exactly the expected 104 unique simulated IDs and
per-cell compartment counts, preserved donor and anatomical provenance, and
the unchanged two supported projections. Build success permits the next
initialization measurement, not a runtime or physiological qualification claim.
Use that measurement and the 40-cell compilation cost to size the 104-cell
compiled run. The four control conditions are required at the final population
size; smaller-stage measurements cannot substitute for them.

The saved build gate will check the raw result against the independent import
audit and topology: exact population membership without duplicates, matching
component member and source/archive hashes per cell, positive integer compartment
counts with a matching total, and exactly the supported enabled contacts. It
will reject incomplete, substituted or internally inconsistent records. This
gate reports construction only and must never label the full readiness goal
complete. Co-located tests cover missing/duplicate cells, changed source hashes,
wrong components, invalid counts and unsupported contacts before the real gate
is used on the 104-cell result.

Gate implementation validation: 27 tests passed, with 100% line coverage of
`docs.evidence.h01_population_build_gate`. Tests also verify the CLI writes
input hashes and exits nonzero on failure. Passing these fixture tests does
not constitute a pass for the real 104-cell build.

Follow-up gate correction: equality alone accepted matching missing hashes.
Two regression tests reproduced that false pass before correction. Source and
archive hashes must now be valid SHA-256 strings as well as equal. Missing,
empty and non-hexadecimal hashes are covered; all 33 gate tests pass with 100%
line coverage. Future provenance checks must validate presence and format before
using equality as evidence.

## First 104-cell construction failure and correction

The first attempt stopped with exit 1 at cell 3812058320 after 587.575 seconds.
Its selected soma sample (22316) lies at branch 2265's endpoint, represented as
1.0000000000000002 by cumulative-length normalization. Electrical intervals
already canonicalize that endpoint to 1.0, so the strict soma-region check
rejects it. The nine-edge branch has eight roughly 0.33605357 um segments and
one roughly 0.28004464 um segment. NumPy's cumulative and total reductions differ
by one floating-point rounding step.

Reproduce the failure using those source coordinates in a small offline fixture.
Canonicalize the final branch fraction to exactly 1.0 when constructing the
anatomy index (the first fraction is already exactly 0.0). Preserve interior
fractions, source geometry, selected soma identity and region membership checks.
Do not move the soma or loosen the region check. Then recheck the real failing
cell and scan all 104 soma/region memberships before rerunning the full build.

## First full-population initialization and compiled smoke

The corrected full build passed the construction gate: 104 cells, 808,495
compartments, two projections, 1,034.034 seconds construction. The next existing
CLI invocation will initialize all 104 cells (per-cell timing and heartbeat),
then run 200 compiled steps at dt 0.005 ms. These phases share one process so
the initialization measurement does not require another redundant build before
stepping. Retain the 1 nA pulse configuration, whose onset at 2 ms lies beyond
this 1 ms pre-stimulus smoke. No activity or delivery verdict follows from it.

The preregistered estimates and limits are in
`docs/evidence/h01-ready-104-ei-1ms-plan.json`. Use the larger of the source-node
ratio and measured compartment ratio to scale the 40-cell initialization,
compilation/stepping and peak memory. Add the measured full construction time.
Launch only if the estimate fits the 3,600-second cap and available memory
exceeds projected peak plus 4 GiB. Retain the 600-second silence cap and live
4 GiB free-memory reserve. Before any runtime pass, independently audit all
104 trace identities, shapes, finite values and end-of-step times.

Supervisor correction: the build's peak-memory accumulator cached an Int32
`Math.Max` overload and failed above 2 GiB; its peak field is invalid. The
independent free-memory checks continued. A 1 MiB-to-5 GiB regression reproduces
the failure; explicit Int64 casts on both arguments and the initial value fix
it. The next run uses those casts. Use the simulator's own peak measurement
and the corrected supervisor observations, never the invalid build peak.

## Full-population runtime evidence gate

Use a reusable checker to bind each run to the hash-verified full construction
reference and its registered duration, dt and control. Require the same 104
identities, cell settings, source provenance, donors and compartment counts;
only the registered control's supported projections may be removed. Verify
all recorded arrays are finite, all cell voltage/output/event arrays have the
expected step and population shape, event arrays are Boolean, and the time grid
is the declared end-of-step grid. Save per-cell extrema, spike counts and times.
Reject missing cells, changed model settings, wrong controls, bad shapes,
nonfinite values and mismatched timing. A pass qualifies only the specified
finite runtime window, never driven activity, physiology or the complete goal.

Runtime-checker validation: 24 tests pass with 100% line coverage, covering
all four controls, missing traces, changed models/contacts, malformed time,
nonfinite voltage/events, incorrect shapes and reference-hash substitution.
The NaN-event regression initially raised while counting already-invalid
events; summaries now use only validated finite arrays. Invalid evidence must
produce a failure verdict without generating invalid summary statistics.
The CLI audits the same trace bytes that it hashes.

## Matched-control delivery checker

Before scoring the four full-population runs, validate each through the runtime
gate against the same construction reference and duration/dt. Require identical
trace keys and time grids. This topology has two disjoint directed pairs; reject
other topologies rather than assuming receiver-only effects in a recurrent graph.
For each removed edge, only its receiver's arrays may differ from `ei`; compare
all other arrays exactly. Every receptor must retain conductance and local
voltage probes in all controls. Disabled receptors must have exactly zero
conductance. Enabled receptors must show a positive conductance rise for each
source event whose delivery fits inside the window, and every rise must match
such an event. Use the declared delay with a conservative two-step timing
allowance for discrete delivery and end-of-step sampling. Reject negative
conductances. Require at least one observable delivery for each supported edge;
silent or too-short windows cannot qualify delivery. This checks delivery and
control isolation only, not functional inhibition or human physiology.

Checker validation: 14 tests pass with 100% line coverage of
`docs.evidence.h01_population_control_gate`. Fixtures cover all four controls,
silent sources, absent or late arrivals, negative or disabled conductance,
missing probes, invalid individual runtime evidence, changed time grids and
effects outside removed receivers. Real matched-control delivery remains
untested until the four hash-linked full-population runs are available.

## E reassessment source audit

Before opening a new physiological hypothesis, check the frozen B3 applied
genomes and pinned source mechanisms. The older reassessment's wording about
absent Kv7/M kinetics must not be used as proof that the model lacks an M
current. Record the nonzero Im rows, source hashes and mechanism definitions
without altering historical decision JSON or launching another evaluation.
Distinguish presence of the source M-current model from unverified human
kinetics and from causal sufficiency for the low-drive response defect.

## First driven full-population window

The 104-cell 1 ms run passed its independent runtime gate. Measured construction,
initialization and compilation/stepping were 1,126.407, 836.600 and 266.922
seconds; peak process RSS was 13,930.617 MiB. Next run the unchanged full model
with `ei`, dt 0.005 ms and duration 10 ms (2,000 compiled steps), retaining the
1 nA soma pulses at 2-5 ms, 10 um CV limit and original two projections.

For preflight, conservatively charge the entire measured compile-and-run cost
ten times, then add measured construction, initialization and process overhead.
This deliberately includes compilation in the scaling term because full-104
stepping throughput has not yet been isolated. Register the exact estimate in
`h01-ready-104-ei-10ms-plan.json`; it is a heuristic, not measured throughput.
Use a 6,000-second wall cap, 600-second silence cap and 4 GiB free-memory reserve.
Project memory as 1.25 times the measured peak plus ten times the in-memory
smoke trace bytes. Check available memory before launch and during execution.
Use this measured driven run to size the remaining controls and dt-halving run.

Require the existing full-population runtime gate, all expected receptor
probes, and a per-cell event table. Record whether each source fires and whether
its receiver shows delayed conductance. No source event means delivery is
untested, not a pass. The four-control comparison remains pending until all
four identical-duration runs exist; a single driven run cannot close it.

## Installed-wheel close-out preparation

Build a wheel from an exact committed source snapshot in a separate cache
directory and install it into a fresh target directory with dependency
installation disabled. Do not modify the live run's interpreter or packages.
Verify installed H01 modules against the wheel and committed Git source bytes,
and resolve imports with Python isolated mode plus only the explicit wheel
target added to its normal dependency paths. Record the wheel hash and origin.
Examples are repository scripts, not shipped wheel modules; the final runtime
check must run the example with library imports pinned to that installed target.
An import/packaging pass alone is not the required final installed-wheel
full-population execution. Preserve that execution gate until measured.

The installed-run launcher will require Python isolated mode, verify the wheel
hash and every installed package payload against that wheel, and reject
production modules already imported from outside the pinned target. It will
record wheel/example hashes before invoking the unchanged example CLI and
record completion or failure afterward, including loaded production origins.
Tests must cover changed wheel bytes, changed installed files, missing inputs,
source-import contamination and failures in the invoked script. This launcher
only establishes execution provenance; its completion flag cannot replace the
trace, control, numerical or physiological gates.

Launcher validation: 23 tests pass with 100% line coverage, including real
isolated subprocesses, payload/hash changes, import contamination, propagated
exceptions/nonzero exits and refusal to overwrite provenance. The in-process
fixture initially supplied arguments only to its mock sys object, while
argparse retained the real sys reference; fixing that fixture made its
coverage meaningful. Keep the real subprocess cases as the independent check.
The actual installed wheel also executes the example's `--help` through this
launcher successfully; this checks CLI wiring, not simulation.

## Driven-window failure and bounded diagnosis

The first 104-cell 10 ms run exits 1 after 2,704.751 seconds: nonfinite output
voltage on cell 7196644737 (1,647 compartments, isolated in the modeled graph).
All 104 cells initialized; the runner raised while converting the returned
traces and saved no trace archive. This is a runtime failure, not a resource
abort and not a pass for the other cells.

First reproduce the evidence-loss defect with a mocked network result: when
one returned array contains NaN, retain all returned arrays and a failed build
record identifying every nonfinite array and its first affected sample. Keep
the CLI exit nonzero. Persist completed initialization metadata before calling
the compiled run, so an exception cannot erase that completed stage. Tests also
cover a compile exception and an ordinary finite result. Do not alter model
dynamics as part of this repair.

Then reproduce cell 7196644737 alone, retaining its exact component, donor,
regions, output site, 1 nA pulse at 2-5 ms, 10 um CV limit and dt 0.005 ms.
Its modeled graph is isolated, so this removes computational load without
removing an input or projection incident on that cell. Bound this diagnostic
to the reproduction and one dt-halving comparison at 0.0025 ms, each at most
600 seconds and with a 4 GiB free-memory reserve. Retain all failed traces.
These diagnose the failure only; full-104 execution must subsequently pass
again. If neither run resolves the failure, inspect its first divergent state
and mechanism before choosing another intervention. No physiological fitting
or anatomy change is authorized by this numerical diagnostic.

Evidence-persistence validation: all 12 regression/CLI tests pass with 100%
line coverage of the example runner. The tests first reproduced the missing
trace archive and lost initialization metadata, then verified retained NaN/Inf
arrays, all affected names, first-sample times (including invalid time values),
nonzero failure exit, finite output conventions and compile exceptions.
Future long runs must checkpoint completed stages and preserve invalid outputs
before rejecting them; rejection must not erase the observations needed for
diagnosis.

The failed isolated runs also exposed a provenance omission: loaded module
origins were recorded only after a successful example return. Regression
assertions reproduce that omission on exceptions and nonzero exits. Record
origins in the failure path too, or record the origin-audit error if imports
are contaminated, while preserving the original model failure. Historical
provenance files remain unchanged; future failures must retain this evidence.

## First-divergent-state observation

After both registered timestep runs reproduce the failure, make one instrumented
reproduction at dt 0.005 ms and 10 ms duration. Add only probes at the existing
output midpoint: calcium concentration/reversal, SK and sodium/potassium gates,
and calcium/potassium/sodium current densities. Record CV area, total modeled
soma membrane area and the source soma radii. Assert exact cell metadata and
unchanged CV geometry before/after probe placement; compare recorded voltage
to the uninstrumented reproduction with a 1e-9 mV allowance before NaN onset.
Preserve all arrays even if invalid. Cap this observation at 600 seconds and
retain the 4 GiB reserve. No timestep, drive, morphology or mechanism change
is part of this observation. Identify the first invalid internal quantity
before proposing a remedy; a very small source radius alone is not causal proof.

The first state observer stopped before initialization because live tuple values
were compared with JSON list values. A reproducing regression distinguishes this
representation difference from a changed interval. The corrected observer compares
the serialized metadata exactly; it does not relax any numeric or geometry value.
A fresh state-r2 label preserves the original failed launch.

## Calcium numerical remedy: bounded scalar validation

The state observation established that the installed frozen-current exponential
update predicts the measured negative calcium. First implement an experimental,
standalone backward-Euler calcium solve with frozen voltage and channel gates,
but concentration-dependent Nernst current inside the solve. Retain the exact
signed source flux, removal, rest concentration, extracellular concentration,
temperature, shell depth and donor gamma; do not clamp calcium or outward current.
Do not change the default solver or any population model in this first stage.

In mM and ms, solve c_new = c_old + dt*(k*(g*1e-3)*(R*log(Co/c_new)-V)
+ (c_rest-c_new)/tau), where g is the total calcium conductance in mS/cm2,
R is the Nernst factor in mV, and k converts inward mA/cm2 to mM/ms.
The residual is strictly increasing in log(c_new) for nonnegative conductance.
Bracket with the old, resting and zero-current Nernst concentrations in log
space; use 64 compiled bisection steps, no concentration floor. Invalid inputs
return NaN and cannot silently become a valid state. This is first-order time
integration, not exact source trajectory equivalence.

Require agreement with independent SciPy roots, strict positivity under the
observed failing conditions, correct inward/outward response, zero-conductance
removal, vectorized JIT execution and first-order refinement against a high
accuracy continuous ODE oracle. Tests must reproduce the original negative
frozen-current update. Only after these pass may a separately registered cell
experiment integrate the kernel, audit current ownership and compare timesteps.
Full population, physiological and installed-wheel qualification remain open.

Scalar validation passed 22 new tests plus the five existing calcium source and
ownership tests, with 100% line coverage of the new kernel. A vectorized timestep
regression exposed an incomplete broadcast before scan: all parameters now
broadcast together before constructing the carry. Future compiled numerical
kernels must test vectorization through every parameter family, not only states.
Edge cases verified include signed flux, no conducting channels, invalid input,
and timestep refinement. Next integration must test runtime calcium ownership,
step ordering, multiple channel contributions and compiled cell parity in the
ordinary regime before the high-voltage single-cell experiment.

## Experimental cell integration

Register a distinct h01_staggered_calcium_implicit solver, never selected by
existing defaults. Preserve the existing scan voltage assembly and family
ordering. Capture old voltage and calcium channel conductance before the voltage
step, integrate other ion self states unchanged, update H01 calcium exactly once
with the tested implicit kernel, then update channel families as before. Reject
non-family ordering. Include every calcium-owned current; verify the aggregate
is ohmic with its dynamic Nernst reversal before applying this specialized solve.
Unsupported current laws must produce invalid evidence, never silently approximate.

First test small compiled cells with calcium and SK, no-calcium equivalence,
multiple calcium contributions and unsupported-order rejection. Compare ordinary
regime timestep refinement. Only after these checks pass run the exact isolated
7196644737 geometry/input at dt .005 and .0025 ms with separate evidence labels,
600-second caps, and retained traces. Changes in solver identity must be explicit
in metadata; all other cell metadata must match. No physiology promotion follows
from numerical finiteness alone.

The first assembled-cell test rejected every calcium step and exposed a units
error in the scalar kernel and its arithmetic oracle: mS/cm2 times mV yields
uA/cm2, so the current must be multiplied by 1e-3 before applying a conversion
factor expressed per mA/cm2. Correct both, and add a BrainUnit dimensional oracle
that cannot share the same bare-number omission. Historical scalar results alone
were insufficient to validate the stated units; the assembled test prevented use
in the population. The observed failure conductance is 1.312167845 mS/cm2.

## Isolated implicit-solver refinement extension

The .005/.0025 ms discrepancy peaks at 5.015 ms, after pulse removal; both
traces begin falling at the first sample after 5 ms. No pulse-boundary repair
is justified by this observation. Register three additional unchanged runs at
.00125, .000625 and .0003125 ms, 10 ms duration, with the same 600-second cap
and 4 GiB reserve per run. Compare consecutive pairs at identical end-step times
without interpolation; require all arrays finite, positive recorded calcium,
identical event count, event timing within .05 ms and voltage error below 1 mV.
Record all comparisons, including failures. Passing this isolated convergence
check does not qualify other cells or the full population. Preserve source
geometry and the assumed 1 nA pulse; do not change input to ease this gate.

Expose the experimental solver through the existing explicit solver argument in
the H01 builder and example CLI. Preserve defaults and record the selected name
in cell metadata. Test explicit selection and forwarding, then obtain a fresh
full104 construction anchor for that solver before runtime validation. Do not
relax runtime-gate metadata equality to reuse an old solver's reference.

## Full104 implicit-solver driven run

After the isolated fine pair passes, run all104 at dt .000625 ms for 10 ms,
control ei, current 1 nA at 2-5 ms, unchanged sources/regions/donors/10 um CV
limit. Use the explicit experimental solver and current evidence-preserving
runner. Cap at 12000 s with 600 s silence limit and 4 GiB reserve. Estimate
~9000 s from the previous full104 build/init plus eight times its 2000-step
runtime; this is a conservative planning estimate, not measured new performance.
Allow 20 GiB peak versus prior ~14 GiB observed runtime. Preserve the construction
checkpoint as a fresh reference, verify it against all104 import/topology sources,
and require exact equality of every cell field except solver with the previous
construction. The runtime gate itself remains unchanged. This run establishes
only driven finiteness; matched controls and fullpopulation refinement remain.

Automate the solver-transition construction check as a separate evidence gate:
require the same ordered 104 identities, donor map, per-cell counts, total CVs,
control and full contact metadata. Require old solver h01_staggered_scan and new
solver h01_staggered_calcium_implicit for every cell, then compare each remaining
cell field exactly. Hash-bind both artifacts. This gate proves unchanged model
metadata apart from solver, not source authenticity or runtime; the existing
construction and runtime gates remain independently required.

The solver-transition checker passes 20 tests with 100% line coverage, including
missing metadata, changed cell geometry/input, wrong solver, population loss or
duplication and CLI failure persistence. The test initially used a relative
import in the non-package docs tree; it now follows the existing absolute
`docs.evidence` import convention. No new production model change accompanied
this checker while the full104 process was running.
