# Calcium-removal midpoint

The third interval is 207.433505 ms at the source removal time and
377.446422 ms at twice that time. The human interval is 307.406282 ms.
Test factor 1.5 as the midpoint of this parameter bracket. Do not
interpret interpolation between these observations as a physical law.

Keep factor-nine geometry, dt 0.000625 ms, command-only input, and the
2100 ms endpoint. Change only the same somatic removal-time parameter.
The reserved 310 pA voltage response must remain uninspected.

Declare a narrow prediction before running: the third-interval absolute
error must be smaller than at both bracket endpoints. The first-interval
absolute error must not exceed that of the source control. The first
peak must remain within 0.1 mV of source, and the first minimum must not
deepen by more than 0.1 mV. Required events must be complete and have
positive peaks. Missing required observations invalidate the comparison.

Retain the second interval and every event time, including any worsening.
These conditions test the bracket and preservation of early behavior;
they do not claim improvement of every physiological target. A five-event
result alone cannot qualify the model. The source, doubled, and midpoint
values are inferred parameters, not human calcium-removal measurements.
