# Keep/drop: every H01 cell tested under its donor's protocol (2026-09-16)

Rule from J: simulate the human cells and let them learn. Cells that work are
kept; cells that do not are dropped. No new fitting, no new donors, no anatomy or
channel tuning to rescue a dropped cell. Parent records: [driven window and
anatomy transfer](2026-09-15-h01-driven-window-and-anatomy-transfer.md) (stage C
measured one cell per donor), [human unity](2026-09-14-h01-human-unity.md).

Executor: Vast 50616476, `/workspace/braintrace`, branch
`campaign/h01-driven-window-20260914`. Receipts under `var/h01-driven/transfer-all-<cell>/`
(and `-dthalf`), copied to `docs/evidence/h01-keep-drop/`.

## Step 1: one run per cell, registered before launch

Runner `docs/evidence/h01_anatomy_transfer_run.py`, unchanged construction path
(`make_h01_ei_cell`, `_regions`, `_register_cell`, MaxCVLen 10 um, implicit calcium
solver, dt 0.005 ms). Cell to donor and polarity from
`docs/evidence/h01-population-types.json` (`rows[*].donor_key`, `polarity`): 57 L2,
22 L4, 18 PV, 7 SST. Protocol per donor, as in stage C:

| Donor | Pulse | Duration | Input | Human count (repeats) | Donor fit count |
| --- | --- | ---: | ---: | --- | ---: |
| l2-pyramidal-allen-541563728 | 1020-2020 ms | 2300 ms | 0.31 nA | 10 | 10 |
| l4-pyramidal-allen-527952884 | 1020-2020 ms | 2300 ms | 0.09 nA | 12 | 8 |
| l5-pv-basket-hl5bn1 | 270-1270 ms | 1500 ms | 0.19 nA | 12 | 14 |
| l3-sst-interneuron-hl5mn1 | 270-1270 ms | 1500 ms | 0.10 nA | 14 (14,14,13,12) | 16 |

One deviation from stage C: the E duration is 2300 ms instead of 2100 ms because
the return rule below reads the trace 200 ms after the pulse ends (2220 ms). The I
protocol already records 230 ms after its pulse. Nothing else changes.

### Keep rule (all five must hold)

1. Finite everywhere (output site and soma, every sample).
2. Rest: the mean output-site voltage over the 100 ms before the pulse is within
   10 mV of the donor recording's rest, and the mean over the 10 ms ending 200 ms
   after the pulse is within 10 mV of the same value (returns there).
3. No -20 mV crossing before the pulse.
4. Count at the primary input inside the human count +- max(2, 30 percent of the
   human count): L2 7-13, L4 9-15, PV 9-15, SST 10-18 (integer counts, absolute
   difference <= max(2, 0.3 x human)).
5. The dt-half repeat (0.0025 ms, same everything else) reproduces the count and
   is finite. The repeat runs only for cells that hold rules 1-4; a cell failing
   any of 1-4 is dropped without it, and a cell whose repeat is missing is not kept.

Donor recording rest (mean of the 100 ms before the pulse in the recording's own
clock, sd in parentheses), measured by `docs/evidence/h01_keep_drop_rest.py` into
`docs/evidence/h01-keep-drop/donor-rest.json` with file hashes:

| Donor | Recording | Rest (corrected) |
| --- | --- | ---: |
| L2 | Allen 541563728 sweep 50, `corrected_voltage_mv` (reported - 14 mV LJP) | -84.26 mV (0.04) |
| L4 | Allen 527952884 NWB `527952752_ephys.nwb` sweep 39, reported - 14 mV | -81.01 mV (0.08) |
| PV | HL5BN1 `human-pv/active-0.npz` as exported | -86.76 mV (0.15) |
| SST | HL5MN1 NWB `571700399_ephys.nwb` sweep 44, reported - 14 mV | -77.15 mV (0.08) |

The L4 and SST NWB files were absent from both caches and were re-fetched from the
Allen well-known-file URLs in the 2026-09-07 acquisition records; their sha256
equal the recorded values.

### Execution

`docs/evidence/h01_keep_drop_chain.py` writes four chain scripts from the types
file (cells sorted by id, dealt round-robin), each run through
`var/h01-driven/run_transfer.sh transfer-all-<cell> 1800 -- ...`, chains pinned
to CPU sets 0-31, 32-63, 64-95, 96-127 and `MEMFRAC=.2` (four processes share the
24 GB GPU; the launcher's `.3` default would over-allocate). Measured stage C
times: 187-254 s per primary run alone, 400-1158 s per dt-half run alone; four
concurrent processes on one GPU were 2x slower in stage C. Cap per primary run
1800 s, per dt-half run 3600 s; a cap hit is untested, not dropped. Population
builds are not run at the same time (thread budget, `ps -eo nlwp` sum under 5,000).

After the primaries: `h01_anatomy_transfer_decision.py keep --phase primary`
lists candidates; the dt-half chain is generated for those only; then the final
`keep` decision writes `docs/evidence/h01-keep-drop/decision.json` with the kept
list, the dropped list and the failed rule for every drop.

Prediction, registered before launch, from stage C: cells on the L4 donor keep,
cells on the L2, PV and SST donors drop (block at -27 mV, plateau at -37 mV,
spontaneous firing). Expected order 20 keep of 104. Every cell is tested; no cell
is dropped by its donor label.

## Steps 2-4

Unchanged from the handoff: kept network driven window with each cell's donor
current for 2-40 ms (registered in a follow-on section before it runs), control
gate at `allclose(atol=1e-5)`, Example 21 grow/prune on the kept manifest with a
60 min cap, write-up, push, sync, stop.

## What this does not claim

A kept cell reproduces a count and a resting level from another human donor's
recording on H01 anatomy. It is not a measurement of the H01 donor, and it does
not qualify channel provenance. The six-term ledger is regenerated over the kept
set with a `kept_cells` field; denominators and tolerances of existing terms are
not changed.
