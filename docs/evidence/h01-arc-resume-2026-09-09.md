# H01 lifecycle stopping point

The user requested a stopping-point notification and notice before multi-hour
runs. No multi-hour run was launched. The full original task is not complete.

Worktree: `.worktrees/h01-example21`, branch `feat/h01-example21-lifecycle`.
The retained `feat/h01-braincell` branch is unchanged by this continuation.

## Implemented and bounded evidence

- Explicit sparse pp-prop follows full multicompartment state dependencies,
  with conservative supports and matrix-free colored JVPs. The default
  compiler guards remain intact. Implicit differentiation uses the unchanged
  DHS tree solve, including its transposed solve for reverse derivatives.
- Four source cells (75,605 compartments) completed two finite grouped Muon
  updates on CPU and GPU. GPU warm synthetic-event update: 56.84 seconds.
- Twelve GPU source cells constructed, initialized, stepped and compiled their
  learning dependency graph. The first update did not finish before the
  900.29-second limit; current RSS was 5.437 GiB. See the raw JSON and its
  authoritative `.limit.json` sidecar.
- H01 topology/history, clone/prune/add operations, stable optimizer remapping,
  asset-verified versioned checkpoints, the evolution adapter and CLI dispatch,
  ordered episode commits and interruption replay are implemented.
- Backend suite: 90 passed, two expected legacy failures, 92% measured coverage.
  Source-equation/compiler/learning subset: 57 passed. Full episode probe fixture:
  passed in 90.60 seconds, including score/update/restore and duplicate-free resume.
- These lifecycle tests use small multicompartment fixtures with real donor
  dynamics. They are not source-population or 104-cell lifecycle qualification.

Initial source topology remains **104 cells, two anatomical contacts, zero
synthetic contacts**. The proposed 102-contact scaffold was not adopted.
Evolution can add explicitly synthetic contacts.

## Cost boundary

The shortest local real training query is `025d127b`, query 0, with 193 advancing
events and 705 events including padding. At the four-cell GPU synthetic-event
rate, 193 events imply about 3.05 hours before construction/compilation. This is
an estimate, not a measured full-episode training cost. Twelve/40/104-cell warm
learning costs and a valid full-campaign estimate remain unmeasured.

Before another run estimated above 15 minutes, present the exact command, wall
limit, memory limit and this evidence for approval. In particular, do not start
a multi-hour episode or eight-round campaign automatically.

## Resume order

1. Resolve twelve-cell update compilation cost under an approved bound. Keep
   candidates sequential. The CPU and GPU package versions differ and are
   independently recorded in each raw probe report.
2. Continue learning construction/compilation/finite-update staging through 40
   and 104. The earlier 104 forward pass does not satisfy the learning gate.
3. Export a content-addressed initial manifest on the runtime that will execute
   it. `examples.h01_arc_manifest` requires successful 104-cell forward evidence;
   it pins installed dependencies and local numerical implementation hashes.
4. Execute `examples.h01_arc_episode_probe` against that manifest with the
   approved resource limits. It loads the unchanged ARC training corpus, scores,
   commits one full episode, restores optimizer/parameters and checks repeated
   scoring. Resume uses the same output directory and durable parent.
5. Execute every mutation on actual source cells, verify coordinator promotion
   and interruption recovery, then measure scoring/learning campaign costs.
6. Finish numerical and causal qualification separately. Native equivalence at
   each timestep is not cross-timestep numerical agreement. A strong passive
   contact fixture currently differs by 15.95 mV (E) / 3.68 mV (I) between the
   two step sizes, although native and adapter match at each size. Diagnose this
   before claiming the 1 mV/1 ms/0.05 ms operating-window gate.

Additional review before production campaigns: protect immutable provenance on
all recovery paths; release old physical runtimes before rebuilding recovered
104-cell models; broaden implementation pins beyond the current numerical
source list; explicitly label the absent-contact ranking's optimizer/activity
proxy; apply process-level resource limits to the production evolution CLI.
The bounded probe commands already enforce wall/RSS limits.

## Validation environment

The host validation interpreter is
`../h01-braincell/.cache/validation/Scripts/python.exe`. At the user's request,
Numba 0.67.0 and llvmlite 0.49.0 were installed there. A compiled Numba function
executed successfully. NumPy 2.5.2, JAX/JAXlib 0.11.1 and BrainState 0.5.4 were
retained. The cached GPU image was not modified.

The first broader oracle/Example 21/coordinator run had 245 passes and 20
failures from missing Numba kernels. An interim `jax_raw` override passed 16
of those 20, but four derivative kernels still selected Numba. Use the actual
installed Numba environment for the closing regression result; do not treat
the incomplete backend override as the normal validation configuration.

Closing normal-backend rerun: **44 passed in 129.89 seconds**, covering all
20 previously failing cases plus their related oracle parameterizations.
No numerical-stack downgrade was needed. The earlier 245 passes include the
existing evolution coordinator and adapter tests. Three probe CLI tests also
passed; they cover task identity, failure evidence and invalid resource limits.

## Commands to prepare, not authorization to run

From the lifecycle worktree, using the selected pinned runtime:

```powershell
python -m examples.h01_arc_manifest --archive ../h01-braincell/.cache/h01/proofread104.zip --output .cache/h01-lifecycle/manifest.json
python -m examples.h01_arc_episode_probe --manifest .cache/h01-lifecycle/manifest.json --arc-root ../../var/arc-agi-1 --output-dir .cache/h01-lifecycle/episode-025d127b --task 025d127b --query 0 --wall-limit-seconds 900 --rss-limit-gib 16
```

The second command's default 15-minute ceiling is expected to stop before a full
source episode completes. Replace it only with a separately approved limit.
Do not create a CPU-pinned manifest and run it on the cached GPU image: restore
correctly rejects their different dependency identities.

Physiology remains unqualified. No actual source-population ARC score or
104-cell encoded ARC learning update has been reported as passing.
